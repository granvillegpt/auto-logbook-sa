/**
 * Local config only. Google API key is set on the server (env GOOGLE_API_KEY).
 * Street geocoding uses POST /api/geocodeAddresses. Place search uses /api/findPlace, /api/textSearch, /api/placeDetails (proxied on the server).
 */
window.GOOGLE_API_KEY = "";

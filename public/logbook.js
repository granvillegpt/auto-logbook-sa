/**
 * Logbook link-based access and token consumption are implemented in `js/logbook-page.js`
 * (loaded by `logbook.html` after Firebase init). This file is kept so the path exists;
 * do not add a script tag for it unless you refactor the load order.
 */

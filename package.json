{
  "name": "auto-logbook-sa",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "sync-engine": "copyfiles -u 1 \"engine/**/*\" public/engine",
    "prestart": "npm run sync-engine",
    "predev": "npm run sync-engine",
    "start": "node server.js",
    "emulators": "firebase emulators:start --only hosting,functions,firestore"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "firebase-admin": "^13.6.0",
    "node-fetch": "^2.7.0"
  }
}
